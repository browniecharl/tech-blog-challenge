<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Tech Blog</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Roboto&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bulma@0.9.0/css/bulma.min.css">
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

  <div class="wrapper">
    <header>
        <h1 class="has-text-black is-size-2 px-3 py-3 has-text-weight-bold">Tech Blog</h1>
        <nav class="navbar">
            {{#if loggedIn}}
            <a href="/" class="navbar-item has-background-color-grey is-hoverable button is-dark pt-1 mx-1">Homepage</a>
            <a href="/dashboard" class="navbar-item button is-dark is-hoverable pt-1">Dashboard</a>
            <a href="/dashboard/create/" class="navbar-item button is-dark is-hoverable pt-1 mx-1">Create post</a>
            <button id="logout" class="button is-dark">Logout</button>
            {{else}}
            <a href="/" class="button is-dark has-text-white mx-3">Homepage</a>
            <a href="/login" class="button is-dark has-text-white mx-3">Login</a>
            {{/if}}
        </nav>
    </header>

    <main class="mx-4">
            {{{ body }}}
    </main>

    <footer class="footer rows pb-2">
        <div class="row-is-full content has-text-centered">
            <p>
            <a href="/" class="button is-dark has-text-white">Homepage</a>{{#if loggedIn}} | 
            <a href="/dashboard/" class="button is-dark has-text-white">dashboard</a> | 
            <a href="/dashboard/create/" class="button is-dark has-text-white">create new post</a>
            {{else}}
            <a href="/login/" class="button is-dark has-text-white">Login</a>
            {{/if}}
            </p>
        </div>
      <div class="row-is-full content has-text-centered">    
  </div>
    </footer>
  </div>

{{#if loggedIn}}
<script src="/javascript/logout.js"></script>
{{/if}}
</body>

</html>

 